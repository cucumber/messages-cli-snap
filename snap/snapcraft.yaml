# Generated with JReleaser 1.23.0 at 2026-04-06T01:05:53.970181647Z
name: cucumber-messages
title: Cucumber Messages CLI
version: "0.3.4"
summary: A commandline interface to work with Cucumber Messages
description: |
  Cucumber can output test results in newline delimited json format. This
  format can then be transformed into different reports. 
  
  This tool supports the conversion of cucumber messages into cucumber-json,
  html, junit-xml and testng-xml reports.

license: MIT AND Apache-2.0 AND (GPL-2.0 WITH Classpath-exception-2.0)

website: https://cucumber.io
source-code: https://github.com/cucumber/messages-cli
contact: https://cucumber.io/community
issues: https://github.com/cucumber/messages-cli
donation: https://opencollective.com/cucumber

grade: stable
confinement: strict
base: core24
type: app

platforms:
  amd64:
    build-on:
      - amd64
    build-for:
      - amd64
  arm64:
    build-on:
      - arm64
    build-for:
      - arm64

apps:
  cucumber-messages:
    command: bin/cucumber-messages
    completer: bin/cucumber-messages_completion
    plugs:
      - home

parts:
  cucumber-messages:
    plugin: dump
    source: https://github.com/cucumber/messages-cli/releases/download/v0.3.4/cucumber-messages-0.3.4.tar.gz
    source-checksum: sha256/d4de9fc749dee63f4c9f756395b436460e3da03b11c7095525c0346174889178
    override-build: |
      jlink \
        --add-modules io.cucumber.messages.cli \
        --launcher cucumber-messages=io.cucumber.messages.cli/io.cucumber.messages.cli.CucumberMessagesCli \
        --no-man-pages \
        --no-header-files \
        --module-path lib \
        --output working-dir
      cp -r working-dir/* $CRAFT_PART_INSTALL
      cp bin/cucumber-messages_completion $CRAFT_PART_INSTALL/bin
    build-packages:
      - openjdk-21-jdk

lint:
  ignore:
    - library:
        - lib/*.so
